const projectPath = '/Users/zzb/work/txbstyle/RN_chinese/ReactNative/';
const storeKey = 'storeKey.js';
const targetFile = `${projectPath}src/config/${storeKey}`;
const outputFile = `./${storeKey}`;

module.exports = {
    projectPath,
    storeKey,
    targetFile,
    outputFile
}